import { IonPage } from '@ionic/react';
import * as React from 'react';
import { useLocation } from 'react-router-dom';

export const Events = () => {
  return (
    <IonPage>
      <h1>Events</h1>
    </IonPage>
  );
};
